{
  "name": "obsidian-uri-triggers",
  "version": "0.1.0",
  "private": true,
  "description": "Obsidian plugin for running Obsidian URIs from configurable vault and workspace triggers.",
  "scripts": {
    "build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.28.0",
    "obsidian": "^1.5.12",
    "typescript": "^5.4.5"
  }
}
